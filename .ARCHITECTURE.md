```mermaid
flowchart TD
    Client[React Client] --> Gateway[API Gateway]

    Gateway --> User[User Service]
    Gateway --> Video[Video Service]

    User --> UserDB[(User PostgreSQL)]
    User --> UserRedis[(Redis)]

    Video --> VideoDB[(Video PostgreSQL)]
    Video --> VideoRedis[(Redis)]

    Video --> S3[S3]
    S3 --> CDN[CloudFront]

    Video --> SQS[SQS]
    SQS --> Worker[Worker]
```